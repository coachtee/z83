<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is only permitted to the local-dev API (the Android
  emulator's alias for the host machine, and localhost for a
  physical device on adb reverse). Everything else stays HTTPS-only —
  see the base-config below with cleartextTrafficPermitted="false".
  A real deployment points ApiClient at an https:// production URL and
  never touches this cleartext exception at all.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
